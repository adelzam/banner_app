<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
          integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.0/jquery.min.js"></script>
    <link rel="import" href="static/header.html">
    <script type="text/javascript" src="/static/js/script.js"></script>
    <link href='/static/css/help_style.css' rel='stylesheet' type='text/css'>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Banner admin panel</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link active" href="/banners">Show all banners <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/audit/my">Show my audit</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/audit">Show audit</a>
            </li>
        </ul>
        <form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <input type="submit" value="Sign Out" class="btn btn-outline-success"/>
        </form>
    </div>
</nav>
<div class="span8">
    Add new banner:
    <form method="post" class="form-horizontal">
        <fieldset>
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <div class="controls">
                <label class="control-label" for="lang_select">Chose local info</label>
                <label>
                    <select name="langId" class="form-control" id="lang_select">
                        {{#locals}}
                            <option value="{{id}}">{{country}} {{language}}</option>
                        {{/locals}}
                    </select>
                </label>
            </div>
            <div class="controls">
                <label class="control-label" for="tagretUrl_input">Set banner target url</label>
                <input type="text" name="targetUrl" placeholder="targetUrl" id="tagretUrl_input"
                       class="input-xlarge">
            </div>
            <div class="controls">
                <label class="control-label" for="tagretUrl_input">Set banner height</label>
                <input type="text" name="height" placeholder="height" class="input-xlarge">
            </div>
            <div class="controls">
                <label class="control-label" for="tagretUrl_input">Set banner width</label>
                <input type="text" name="width" placeholder="width" class="input-xlarge">
            </div>
            <div class="controls">
                <label class="control-label" for="tagretUrl_input">Set banner image source</label>
                <input type="text" name="imgSrc" placeholder="imgSrc" class="input-xlarge"></div>
            {{#errors}}{{.}}{{/errors}}
            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Add new banner</button>
            </div>
        </fieldset>
    </form>
</div>
<div style="width: 96%; padding: 0; border: 0; margin: 2%;">
    <table class="table table-striped table-bordered">
        <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Country</th>
            <th scope="col">Language</th>
            <th scope="col">TargetUrl</th>
            <th scope="col">height</th>
            <th scope="col">width</th>
            <th scope="col">image source</th>
            <th scope="col" colspan="3" align="center">
                <form method="post" action="/banners/filter">
                    <input type="hidden" name="_csrf" value="{{_csrf.token}}">
                    <label>
                        <select name="sortType" class="form-control">
                            {{#filters}}
                                <option value="{{key}}">{{value}}</option>
                            {{/filters}}
                        </select>
                    </label>
                    <button type="submit" class="btn btn-default btn-lg">Sort</button>
                </form>
            </th>
        </tr>
        </thead>
        <tbody>
        {{#banners}}
            <tr>
                <th scope="row">{{id}}</th>
                <td>{{lang.country}}</td>
                <td>{{lang.language}}</td>
                <td>{{targetUrl}}</td>
                <td>{{height}}</td>
                <td>{{width}}</td>
                <td>{{imgSrc}}</td>
                <td>
                    <form action="/banners/edit/{{id}}">
                        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
                        <button type="submit" class="btn btn-default btn-lg">Edit</button>
                    </form>
                </td>
                <td>
                    <form action="/banners/delete/{{id}}">
                        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
                        <button type="submit" class="btn btn-default btn-lg">Delete</button>
                    </form>
                </td>
                <td>
                    <form action="/audit/banner/{{id}}">
                        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
                        <button type="submit" class="btn btn-default btn-lg">Show change history</button>
                    </form>
                </td>
            </tr>
        {{/banners}}
        </tbody>
    </table>
</div>
</body>
</html>